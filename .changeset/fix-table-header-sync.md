---
"@ricsam/formula-engine": patch
---

Keep table column definitions and structured references synchronized when headers change, populate blank headers with unique default names, and reject duplicate table column names.
